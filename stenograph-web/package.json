{
  "name": "stenograph-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test:codecs": "node scripts/codec-smoke.mjs",
    "deploy:worker": "npm run build && npx wrangler deploy --name stenograph --assets=dist --compatibility-date 2026-05-21",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
